<h1>Courses</h1>

<p><a href="/new_course">Add a New Course</a></p>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @courses.each do |course| %>
    <tr>
          <td><%= course.name %></td>
          <td><button><a href="/courses/<%= course.id %>">Show</a></button></td>
      <td></button><a href="/courses/<%= course.id %>/edit"><button>Edit</a></td>
      <td><button><a href="/courses/<%= course.id %>/destroy">Destroy</a></button></td>
    </tr>
  <% end %>
  </tbody>
</table>

